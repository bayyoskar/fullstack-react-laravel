[build]
builder = "nixpacks"
buildCommand = "cd ubur_ubur && composer install --no-dev --optimize-autoloader && php artisan key:generate"
startCommand = "cd ubur_ubur && php artisan serve --host=0.0.0.0 --port=8000"